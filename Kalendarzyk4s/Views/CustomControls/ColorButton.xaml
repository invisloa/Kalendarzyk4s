<?xml version="1.0" encoding="utf-8" ?>
<Frame xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Kalendarzyk4s.Views.CustomControls.ColorButton">
    <Button x:Name="Button" BorderWidth="{Binding BorderWidth}" BackgroundColor="{Binding Color}" Command="{Binding ColorSelectionCommand}" CommandParameter="{Binding ColorCode}" Margin="0"/>

</Frame>
